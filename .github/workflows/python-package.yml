name: Build and Run Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-docker:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: Terminalich check
        run: ls -la

      - name: 🐳 Use Docker
        run: |
          docker build -t my-hello-friend .
          ddocker push daniil14/my-hello-friend:latest
        
          
        
      
      
        
      
